<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <script src="https://kit.fontawesome.com/c554defd4c.js" crossorigin="anonymous"></script>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
            <nav class="navbar navbar-expand-lg navbar-dark bg-warning">
              <div class="container">
                <a class="navbar-brand" href="/">
                  <span>NAGANO Cake</span>
                </a>
        <% if user_signed_in? %>



          <span class="navbar-toggler-icon"></span>
            <div class="collapse navbar-collapse",id="navbarNavDropdown">
              <ul class="navbar-nav ml-auto">
                <li>
                  <%= link_to " My page", users_path,class: 'fas fa-home nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " Tasty Cakes", items_path,class: 'fas fa-users nav-link text-light'%>
                </li>
                <li>
                  <%= link_to " Cart", cart_items_path,class: 'fas fa-book-open nav-link text-light'%>
                </li>
                <li>
                  <%= link_to " logout", destroy_user_session_path,method: :delete,class: 'fas fa-sign-out-alt nav-link text-light'%>
                </li>
              </ul>
            </div>

        <% elsif admin_signed_in?%>

            <div class="collapse navbar-collapse",id="navbarNavDropdown">
              <ul class="navbar-nav ml-auto">
                <li>
                  <%= link_to " 商品一覧", admin_other_items_path,class: 'fas fa-link nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " 会員一覧", admin_other_users_path,class: 'fas fa-link nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " 注文履歴一覧", admin_other_orders_path,class: 'fas fa-link nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " ジャンル一覧", admin_other_genres_path,class: 'fas fa-link nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " ログアウト", destroy_admin_session_path,method: :delete,class: 'fas fa-link nav-link text-light' %>
                </li>
             </ul>
            </div>

        <% else %>

            <div class="collapse navbar-collapse",id="navbarNavDropdown">
              <ul class="navbar-nav ml-auto">
                <li>
                  <%= link_to " About", "/homes/about",class: 'fas fa-link nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " Tasty Cakes", items_path,class: 'fas fa-candy-cane nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " sign up", new_user_registration_path,class: 'fas fa-user-plus nav-link text-light' %>
                </li>
                <li>
                  <%= link_to " login", new_user_session_path,class: 'fas fa-sign-in-alt nav-link text-light' %>
                </li>
              </ul>
              </div>

        <% end %>

        </div>
        </nav>
        </header>

        <main>
        <% flash.each do |message_type, message| %>
        <%= message %>
        <% end %>
        <%= yield %>
        </main>

        <footer>
          <div class="container mt-5">
          <div class="row">
          <div class="mx-auto">
         <p>Nagano Cake,Ltd.</p>
          </div>
          </div>
          </div>
        </footer>
      </body>

</html>
