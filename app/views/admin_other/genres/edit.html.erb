<div class="container mt-5">
<div class="row">
<div class="mx-auto">

<h2>ジャンル編集</h2>
    <div>
      <%= form_with model: @genre,url: admin_other_genre_path, local:true do |f| %>
    <div class="form-group">
      <%= f.label :name, "ジャンル"%>
      <%= f.text_field :name, autofocus: true %>
      <%= f.submit "変更を保存する", class: "btn btn-primary" %>
    </div>
      <% end %>

</div>
</div>
</div>